import { rule as receiveObject } from "./receive-object.js";
import { rule as returnObject } from "./return-object.js";

export const rules = {
  "receive-object": receiveObject,
  "return-object": returnObject,
};
